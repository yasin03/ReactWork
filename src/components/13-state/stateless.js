import React from "react";
import { Button } from "reactstrap";

const Stateless = () => {
  return (
    <div className="p-5 d-flex justify-content-around">
      <Button>Dark zvzxvMode</Button>
      <Button>Light Mode</Button>
    </div>
  );
};

export default Stateless;
